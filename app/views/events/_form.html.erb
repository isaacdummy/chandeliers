<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :killer_id %><br>
    <%= f.collection_select :killer_id, Person.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :victim_id %><br>
    <%= f.collection_select :victim_id, Person.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :event_type_id %><br>
    <%= f.collection_select :event_type_id, EventType.all, :id, :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
